import { Sigmar, Kanit, Cormorant_Garamond } from "next/font/google";

export const sigmar = Sigmar({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

export const kanit = Kanit({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

export const cormorant = Cormorant_Garamond({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});
